# Nombre del workflow
name: Deploy Backend and Frontend to Server

# Se activa con un push a la rama main
on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy Backend & Frontend
    runs-on: ubuntu-latest
    
    steps:
      # 1. Descargar el código del repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Instalar la clave SSH
      - name: Install SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      # 3. Sincronizar SIEMPRE la carpeta backend al servidor
      - name: Sync backend files via rsync
        run: |
          rsync -avzr --delete \
            -e "ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
            ./backend/ \
            ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }}:/root/quimex_2.0/sistema_quimicos/

      # 4. Sincronizar SIEMPRE la carpeta frontend al servidor
      - name: Sync frontend files via rsync
        run: |
          rsync -avzr --delete \
            -e "ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
            ./frontend/ \
            ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }}:/root/quimex_2.0/frontend/

      # 5. Instalar dependencias y reiniciar SIEMPRE el backend
      - name: Install backend dependencies and restart service
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Archivos backend sincronizados. Preparando entorno..."
            cd /root/quimex_2.0/sistema_quimicos

            if [ ! -d "venv" ]; then
              echo "⚙️ Creando entorno virtual..."
              python3 -m venv venv
            fi

            source venv/bin/activate

            if [ -f requirements.txt ]; then
              echo "📦 Instalando dependencias..."
              pip install --upgrade pip
              pip install --upgrade -r requirements.txt
            fi

            deactivate

            echo "🔄 Reiniciando servicio backend..."
            sudo systemctl restart quimex.service
            echo "✅ Backend desplegado y servicio reiniciado con éxito."

      # 6. Instalar dependencias y hacer build SIEMPRE del frontend
      - name: Install frontend dependencies and build
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Archivos frontend sincronizados. Preparando build..."
            cd /root/quimex_2.0/frontend

            if [ -f package.json ]; then
              echo "📦 Instalando dependencias frontend..."
              npm install
            fi

            echo "⚡ Compilando frontend..."
            npm run build

            echo "📂 Moviendo build a /var/www/quimex_frontend..."
            sudo rm -rf /var/www/quimex_frontend/*
            sudo cp -r build/* /var/www/quimex_frontend/

            echo "✅ Frontend compilado y desplegado."