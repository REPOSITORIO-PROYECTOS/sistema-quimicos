# Nombre del workflow
name: Deploy Backend and Frontend to Server

# Se activa con un push a la rama main si hay cambios en backend o frontend
on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'

jobs:
  deploy:
    name: Deploy Backend & Frontend
    runs-on: ubuntu-latest
    
    steps:
      # 1. Descargar el código del repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Instalar la clave SSH
      - name: Install SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      # 3. Sincronizar la carpeta backend al servidor
      - name: Sync backend files via rsync
        if: ${{ github.event.head_commit.modified && contains(join(github.event.head_commit.modified, ' '), 'backend/') }}
        run: |
          rsync -avzr --delete \
            -e "ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
            ./backend/ \
            ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }}:/root/quimex_2.0/sistema_quimicos/

      # 4. Sincronizar la carpeta frontend al servidor
      - name: Sync frontend files via rsync
        if: ${{ github.event.head_commit.modified && contains(join(github.event.head_commit.modified, ' '), 'frontend/') }}
        run: |
          rsync -avzr --delete \
            -e "ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
            ./frontend/ \
            ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }}:/root/quimex_2.0/frontend/

      # 5. Instalar dependencias y reiniciar backend
      - name: Install backend dependencies and restart service
        if: ${{ github.event.head_commit.modified && contains(join(github.event.head_commit.modified, ' '), 'backend/') }}
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Archivos backend sincronizados. Preparando entorno..."
            cd /root/quimex_2.0/sistema_quimicos

            # Crear venv si no existe
            if [ ! -d "venv" ]; then
              echo "⚙️ Creando entorno virtual..."
              python3 -m venv venv
            fi

            # Activar entorno
            source venv/bin/activate

            # Actualizar dependencias si existe requirements.txt
            if [ -f requirements.txt ]; then
              echo "📦 Instalando dependencias..."
              pip install --upgrade pip
              pip install --upgrade -r requirements.txt
            fi

            deactivate

            # Reiniciar servicio
            echo "🔄 Reiniciando servicio backend..."
            sudo systemctl restart quimex.service
            echo "✅ Backend desplegado y servicio reiniciado con éxito."

      # 6. Instalar dependencias y hacer build del frontend
      - name: Install frontend dependencies and build
        if: ${{ github.event.head_commit.modified && contains(join(github.event.head_commit.modified, ' '), 'frontend/') }}
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Archivos frontend sincronizados. Preparando build..."
            cd /root/quimex_2.0/frontend

            # Instalar dependencias de npm
            if [ -f package.json ]; then
              echo "📦 Instalando dependencias frontend..."
              npm install
            fi

            # Build de producción
            echo "⚡ Compilando frontend..."
            npm run build

            # Copiar build a la carpeta de Nginx
            echo "📂 Moviendo build a /var/www/quimex_frontend..."
            sudo rm -rf /var/www/quimex_frontend/*
            sudo cp -r build/* /var/www/quimex_frontend/

            echo "✅ Frontend compilado y desplegado."
